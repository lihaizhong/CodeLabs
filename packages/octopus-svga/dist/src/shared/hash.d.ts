/**
 * 简易的hash算法
 * @param buff
 * @param start
 * @param end
 * @param step
 * @returns
 */
export declare function calculateHash(buff: Uint8Array, start: number, end: number, step: number): string;
