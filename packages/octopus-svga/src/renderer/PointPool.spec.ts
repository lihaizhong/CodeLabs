import { PointPool } from "./PointPool";

describe("PointPool 定义", () => {
  it("PointPool 是否被定义", () => {
    expect(PointPool).toBeDefined();
  });

  it("PointPool 定义是否正确", () => {});

  it("PointPool 实例方法", () => {});
});
