import { Parser } from ".";

describe("Parser 定义", () => {
  it("Parser 是否被定义", () => {
    expect(Parser).toBeDefined();
  });

  it("Parser 定义是否正确", () => {});

  it("Parser 静态方法", () => {});

  it("Parser 实例方法", () => {});
});
