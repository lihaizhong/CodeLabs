import { Player } from ".";

describe("Player 定义", () => {
  it("Player 是否被定义", () => {
    expect(Player).toBeDefined();
  });

  it("Player 定义是否正确", () => {});

  it("Player 静态方法", () => {});

  it("Player 实例方法", () => {});
});
