import { Config } from "./config";

describe("Config 定义", () => {
  it("Config 是否被定义", () => {
    expect(Config).toBeDefined();
  });

  it("Config 定义是否正确", () => {});

  it("Config 静态方法", () => {});

  it("Config 实例方法", () => {});
});
