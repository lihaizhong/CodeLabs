import { Poster } from ".";

describe("Poster 定义", () => {
  it("Poster 是否被定义", () => {
    expect(Poster).toBeDefined();
  });

  it("Poster 定义是否正确", () => {});

  it("Poster 静态方法", () => {});

  it("Poster 实例方法", () => {});
});
