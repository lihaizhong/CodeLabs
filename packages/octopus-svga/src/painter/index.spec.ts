import { Painter } from ".";

describe("Painter 定义", () => {
  it("Painter 是否被定义", () => {
    expect(Painter).toBeDefined();
  });

  it("Painter 定义是否正确", () => {});

  it("Painter 静态方法", () => {});

  it("Painter 实例方法", () => {});
});
