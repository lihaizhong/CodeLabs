declare function render(context: PlatformRenderingContext2D, materials: Map<string, Bitmap>, dynamicMaterials: Map<string, Bitmap>, videoEntity: Video, currentFrame: number, head: number, tail: number, globalTransform?: Transform): void;
export default render;
