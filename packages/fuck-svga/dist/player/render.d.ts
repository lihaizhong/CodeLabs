declare function render(context: PlatformRenderingContext2D, materials: Map<string, Bitmap>, videoEntity: Video, currentFrame: number, head: number, tail: number, globalTransform?: GlobalTransform): void;
export default render;
