import { Brush } from ".";

describe("Brush 定义", () => {
  it("Brush 是否被定义", () => {
    expect(Brush).toBeDefined();
  });

  it("Brush 定义是否正确", () => {});

  it("Brush 静态方法", () => {});

  it("Brush 实例方法", () => {});
});
