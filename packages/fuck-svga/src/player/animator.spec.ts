import { Animator } from './animator';

describe("Animator 定义", () => {
  it("Animator 是否被定义", () => {
    expect(Animator).toBeDefined();
  });

  it("Animator 定义是否正确", () => {});

  it("Animator 静态方法", () => {});

  it("Animator 实例方法", () => {});
});
