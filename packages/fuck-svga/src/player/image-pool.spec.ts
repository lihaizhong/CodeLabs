import { ImagePool } from "./image-pool";

describe("ImagePool 定义", () => {
  it("ImagePool 是否被定义", () => {
    expect(ImagePool).toBeDefined();
  });

  it("ImagePool 定义是否正确", () => {});

  it("ImagePool 实例方法", () => {});
});
